# cap.py
def cap_text(text):
    """
    cap_text will capitalize the first letter
    of the each word in a string that is passed in
    :arg text (string)
    :return string
    """
    return text.title()
